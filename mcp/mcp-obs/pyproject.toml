[project]
name = "mcp-obs"
version = "0.1.0"
dependencies = ["mcp>=1.0.0", "httpx>=0.27.0"]

[project.scripts]
mcp-obs = "mcp_obs.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
